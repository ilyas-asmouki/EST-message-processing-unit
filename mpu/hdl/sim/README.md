# Simulation Configurations

Simulator-specific run areas live here (e.g., `questa/` vs `verilator/`). Each folder can host do files, wave setups, and regression scripts. Testbenches pull RTL from `../rtl` and automation from `../scripts`, keeping tool noise out of the source tree.
